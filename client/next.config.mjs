/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["res.cloudinary.com", "media.istockphoto.com", "encrypted-tbn0.gstatic.com"],
  },
};

export default nextConfig;
